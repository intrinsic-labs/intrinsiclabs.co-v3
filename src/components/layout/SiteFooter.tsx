export function SiteFooter() {
  return (
    <footer className="border-t border-border-subtle">
      <div className="container-shell flex flex-col gap-3 py-10 lg:flex-row lg:items-center lg:justify-between">
        <p className="mono-label type-xs text-cream-muted">
          Intrinsic Labs · New Build Track
        </p>
        <p className="mono-label type-xs text-ink-dim">
          Homepage direction lock-in phase
        </p>
      </div>
    </footer>
  );
}
